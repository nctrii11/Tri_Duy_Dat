returns:
  kind: "log"  # log, arith
  window: 1
  freq: "D"  # D for daily, M for monthly
  freq_markowitz: "M"  # Monthly for Markowitz

cov:
  shrinkage: true
  shrinkage_factor: 0.2
  jitter: 1e-6  # Small value added to diagonal

